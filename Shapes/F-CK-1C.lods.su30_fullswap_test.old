model = 
{
    lods = {
        {"F-CK-1C.edm", 10000.0};
    };
    -- Original collision shell restored for valid testing.
    collision_shell = "idf_hitbox.edm";
    -- Previous test kept for reference only. DCS rejected it with
    -- "More distant lod must not have more arguments than closer"
    -- and then flagged the unit damage model as corrupt.
    -- collision_shell = "su-30mk-collision_TEST.edm";
}
